{
  "name": "jood-v-modal",
  "private": true,
  "workspaces": [
    "packages/*",
    "document"
  ],
  "description": "",
  "license": "MIT",
  "scripts": {
    "bootstrap": "yarn run bootstrap:install && yarn run package:build",
    "bootstrap:install": "yarn install --production=false",
    "package:build": "yarn run v-modal:build",
    "package:test:unit": "yarn run v-modal:test",
    "v-modal:build": "yarn workspace @jood/v-modal build",
    "v-modal:test": "yarn workspace @jood/v-modal test",
    "docs:build": "yarn workspace document docs:build"
  },
  "keywords": [],
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.3.4",
    "@types/jest": "^28.1.6",
    "esbuild": "^0.14.51",
    "jest": "^28.1.3",
    "jest-environment-jsdom": "^28.1.3",
    "jsdom-testing-mocks": "^1.4.0",
    "rollup": "^2.77.2",
    "rollup-plugin-dts": "^4.2.2",
    "rollup-plugin-esbuild": "^4.9.1",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.4"
  }
}
